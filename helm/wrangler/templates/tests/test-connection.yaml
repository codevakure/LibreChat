apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wrangler.fullname" . }}-test-connection"
  labels:
    {{- include "wrangler.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wrangler.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
