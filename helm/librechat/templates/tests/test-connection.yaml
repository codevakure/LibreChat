apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pleach.fullname" . }}-test-connection"
  labels:
    {{- include "pleach.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pleach.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
